__base = __base || '../';
var c = require(__base + 'constants');
var log = require(__base + 'utils/log')('utils/validate-model');

function validateModel(d, schema){
    
    return d;
}

module.exports = validateModel;